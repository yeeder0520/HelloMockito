package com.tradevan.demo.user;

public class UserRepository {

  public void saveUser(UserRecord userRecord) {
    // save user record
  }

  public UserRecord getUserById(UserRecord userRecord) {
    return new UserRecord();
  }

}
